* symlink `init.vim`, `post_init.lua`, and `cheatsheet.md` into the config folder, normally `~/.config/nvim/`
* add a `local.vim` to the config folder for local only config
* install [vim-plug](https://github.com/junegunn/vim-plug)
* run `:PlugInstall`
* deal with inevitable issues around missing native packages etc.
* press `,,` inside nvim for a cheatsheet of bindings
